# Briscas

Browser-based Briscas game built with Vite, React, TypeScript, Firebase Firestore, Vitest, and VitePress.

## Quick Start

```bash
npm install
npm run dev
```

Local human-vs-AI mode works without online configuration. For online rooms, copy `.env.example` to `.env` and fill the public web config.

## Verify

```bash
npm run test:run
npm run build
npm run docs:build
```

## Documentation

```bash
npm run docs:dev
```

See `docs/` for architecture, domain model, SOLID mapping, Firestore schema, UML diagrams, and deployment instructions.
